<?php
// Retrieve the product ID from the query parameter
$product_id = $_GET['product_id'];

echo $product_id;

// Use the product ID to fetch and handle the product data
// For example:
// $product_data = fetch_product_data($product_id);

// Output the product data
// echo $product_data;
?>
